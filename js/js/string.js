'use strict';

function stringTest() {
  builtInFunctions();
}

function builtInFunctions() {
  // Get string length
  var myStr = 'hello';
  console.log('myStr length: ' + myStr.length);

  // Convert to upper case
  console.log('myStr in upper case: ' + myStr.toUpperCase());
}
